# Presentation review fixes

Read this first; it is the bounded requirements for this task. The project is a local Phaser3+TypeScript+Vite stealth game. No git repository, backend, remote assets, deployment or new dependencies. Do not spawn subagents. Preserve existing campaign/core files.

Own: src/game/ui/AppUI.ts, src/game/ui/Controls.ts if needed, src/game/scenes/GameScene.ts, src/game/systems/AudioManager.ts and HintService.ts, src/main.ts, src/style.css, tests for these modules, README.md. Do not edit Simulation/Geometry/levels/campaign tests/browser-harness.

Fix findings with targeted RED→GREEN tests, avoiding untestable private-state assertions:

1. Pause/result dialogs need modal input isolation: gameplay background inert, Tab/Shift+Tab wrap inside current dialog, no backdrop HUD actions via click/keyboard. Restore normal control input on resume/retry. Prevent duplicate modal nodes. Assert these with real happy-dom UI transitions and keyboard events. Keep current large mobile controls and visual design.
2. Short-height safe areas: max-height640px overrides must retain env(safe-area-inset-top/bottom) for menu/page/HUD/status/energy/touch region and modal. Keep layout fitting320×568. Heading strings lose spaces when CSS hides br: `Find yourway out.` and `A quieterkind of fear.` Fix semantic spacing.
3. Paused audio: normal pause currently leaves hum/metal running. Suspend actual AudioContext on pause and blur, resume after explicit game resume/gesture. Muting should suspend context too. Avoid paused settings clicks inadvertently resuming gameplay ambience. Retain sound feedback/menu audio where appropriate; no errors when WebAudio unavailable. Test lifecycle with a small AudioContext-shaped fake; don't mock internal methods as the assertion.
4. Alert freeze currently skips Simulation.step for .045sec, delaying light release and resuming enemy live tracking despite release. Preserve short visible freeze while sampling/applying light state at every simulation tick. Prefer a render-only freeze of spatial presentation over mutation of Simulation internals. Light release must be immediate at next fixed timestep even during alert. Add a regression around actual orchestration (can extract a small pure stepping helper; avoid source string assertions).
5. Hints: GameScene passes solution without player spawn; most later levels solution arrays exclude spawn. nextWaypoint can then point across a wall to the end of the next segment. Prepend spawn and ensure hint direction from off-route positions follows collision-safe path (Geometry.findPath exists). Do not promise safe enemy timing; safe means geometry-safe. Cover route without initial spawn, an off-route corner, and existing async authorization. Keep public interfaces simple.
6. FPS config: Phaser TimeStep.stepLimitFPS in installed source resets delta=0 instead of preserving remainder, leading to ~48 rendered frames on144Hz under limit60. Remove hard limit while retaining target60 and fixed60Hz Simulation; update README cap→target. No invented benchmark claims.

7. Browser Listener QA: fast movement in dark correctly triggers pursuit/capture, but the result's advice is always `Let the light go.` even with zero light usage. Preserve the required CAUGHT IN THE LIGHT headline; provide Listener-specific body advice about slow movement when the pursuer is a Listener. In a dark Listener awareness episode the HUD should explain hearing rather than only vague memory. Cover with real UI state fixtures; avoid changing core AI/state contracts solely for wording.

Report to docs/qa/presentation-fixes-report.md: status, changed files, exact tests/commands+RED→GREEN outputs, self-review concerns. Run targeted tests and npm run build, not full campaign (it is owned separately). npm commands need require_escalated due Windows parent directory sandbox; use npm test / npm run build prefixes. No commits available. Return concise status/test summary only.
